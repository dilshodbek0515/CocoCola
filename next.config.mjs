/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "storage.kun.uz",
            "media.istockphoto.com",
            "cdn.dummyjson.com",
        ]
    }
};

export default nextConfig;
